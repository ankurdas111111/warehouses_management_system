# Local Sidekiq → Remote services (Render Postgres external URL + Upstash Redis).
#
# Copy to `.env.sidekiq-remote` (gitignored) and fill values.
#
# Required:
RAILS_ENV=production
DATABASE_URL=
# Upstash uses TLS, so use rediss:// (not redis://):
# REDIS_URL=rediss://default:<PASSWORD>@select-antelope-31615.upstash.io:6379/0
REDIS_URL=
RAILS_MASTER_KEY=
#
# Optional:
SIDEKIQ_CONCURRENCY=5
RAILS_LOG_LEVEL=info
SIDEKIQ_CRON_ENABLED=1
AUTO_FULFILL_ENABLED=1


