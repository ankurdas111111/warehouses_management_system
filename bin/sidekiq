#!/usr/bin/env bash
set -euo pipefail

# Local default matches docker-compose.yml mapping (host 6380 -> container 6379).
export REDIS_URL="${REDIS_URL:-redis://localhost:6380/0}"

exec bundle exec sidekiq -C config/sidekiq.yml


